[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt && playwright install chromium --with-deps"

[deploy]
startCommand = "python iranfly_bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
